[build-system]
requires = ["setuptools>=65.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "srgan-project"
version = "1.0.0"
description = "SRGAN Image Enhancement Project"
requires-python = ">=3.9,<3.10"
